# Server-side DKOS ingestion worker requirements
# Install these in the backend/worker runtime, not in the Vite frontend bundle.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.8.0

markitdown>=0.1.0
# Enable docling in the worker image when the target runtime supports it.
# docling>=2.0.0

pillow>=10.0.0
pandas>=2.2.0
openpyxl>=3.1.0
python-docx>=1.1.0
pymupdf>=1.24.0
tiktoken>=0.7.0

pinecone>=5.0.0
supabase>=2.31.0
httpx>=0.27.0

# .npmrc — npm configuration for Devonn.AI
#
# legacy-peer-deps: required to resolve jsdom@23's optional canvas peer dep
# without triggering the Arborist null-node edgesOut bug on Node 22 / npm 10.9.
# See: https://github.com/npm/cli/issues/7703
legacy-peer-deps=true

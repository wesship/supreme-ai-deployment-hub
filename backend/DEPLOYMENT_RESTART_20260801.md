# Production Backend Restart Marker

This documentation-only marker was added on 2026-08-01 to trigger the repository-connected Railway production backend deployment after rotating `OPENAI_API_KEY`.

It changes no application behavior, dependencies, configuration, or runtime logic.

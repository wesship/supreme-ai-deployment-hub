"""Safety-first Jetson Control domain primitives.

This package intentionally contains pure contracts and validation only. Device
execution must remain behind an authenticated backend adapter and must never be
invoked directly from the browser.
"""

# backend/requirements.txt — Devonn.AI FastAPI Backend
# Pin all production dependencies for deterministic builds.
# Update with: pip-compile requirements.in

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.5
PyJWT>=2.8.0
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1

# ── Database ───────────────────────────────────────────────────────────────
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36

# ── Caching ────────────────────────────────────────────────────────────────
redis==5.2.0
hiredis==3.0.0

# ── Authentication ─────────────────────────────────────────────────────────
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.27

# ── HTTP client ────────────────────────────────────────────────────────────
httpx==0.27.2

# ── AI / LLM ───────────────────────────────────────────────────────────────
openai==1.55.3

# ── Observability ──────────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.18.0
prometheus-fastapi-instrumentator==7.0.0

# ── Task queue ─────────────────────────────────────────────────────────────
celery==5.4.0
kombu==5.4.2

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv==1.2.2
structlog==24.4.0
tenacity==9.0.0

# ── Testing (dev only — install with: pip install -r requirements-dev.txt) ─
# pytest==8.3.3
# pytest-asyncio==0.24.0
# pytest-cov==6.0.0
# httpx==0.27.2  (already above)
# factory-boy==3.3.1

# Hermes Model Council package

Phase 1 is feature-gated by `HERMES_MODEL_COUNCIL_ENABLED=false` by default.

The package is provider-neutral and intentionally reuses the existing Hermes workflow runtime rather than introducing a second scheduler or queue.

# Hermes Agent v0.20.5

Upstream release pinned to `v2026.8.19`. Verify with `hermes --version` after staging installation. Do not replace D3VONN's existing Hermes worker until compatibility testing passes.
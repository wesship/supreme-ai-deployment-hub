# Hermes Staging Connector Access — 2026-08-18

The session has no configured Railway connector. The Vercel connector was enabled but did not expose an executable MCP server in this session. The public Vercel preview URL for the PR redirects to Vercel SSO, so it cannot be used as an unauthenticated staging-gate target.

The connected browser successfully opened the Railway dashboard at `https://railway.com/dashboard` and rendered the authenticated dashboard shell. The Hermes worker health-check configuration can therefore be inspected and, after the code and database remediation is validated, corrected through the Railway dashboard with the user’s authorization for the already requested staging repair.

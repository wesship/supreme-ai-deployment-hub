# Next gate: provider qualification

The next gate pins exact upstream revisions and weight hashes for each provider, verifies licenses and dependencies, benchmarks private GPU targets, runs controlled generation and audio-QA smoke tests, and records reviewer approval before any provider can be enabled.

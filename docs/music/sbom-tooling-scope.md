# SBOM tooling scope

The repository tool validates and normalizes a CycloneDX file that was produced elsewhere from an exact runtime image. It performs no registry login, model download, image pull, or GPU execution.

# Phase B — Workflow Audit Findings

Generated by `scripts/workflow-audit/{dead-paths,permissions-audit}.sh`.

## Headline numbers (106 workflows)

| Signal | Count |
|---|---|
| Dead-path references | **23** |
| Missing top-level `permissions:` block | **31** |
| `write-all` granted anywhere | 0 |
| Deploy-capable (`id-token: write` + `contents: write`) | 4 |
| Secret-heavy (≥5 distinct secrets) | 10 |

## Dead paths — quick wins

Almost every dead reference is a `develop` branch that no longer exists in this repo (only `main` lives). Two cohorts:

**1. `develop` branch references (16 workflows)** — strip `develop` from `on.push.branches` and `on.pull_request.branches`:
- bundle-size, code-quality-gates, codeql, commitlint, coverage-enforcement, e2e, hermes-gate, hermes-v3-gate, infrastructure-ci-cd, lighthouse, testing

**2. Missing scripts / Dockerfiles / branches (real bugs):**
- `infrastructure-ci-cd.yml` → `scripts/generate-tf-files.js` (missing)
- `terraform.yml` → `scripts/verify-completion.sh` (missing)
- `developer-onboarding.yml` → `Dockerfiles`, `SERVICE/Dockerfile` (placeholders left in)
- `trusted-runner-isolation.yml` → `/tmp/runner-image/Dockerfile.runner`, `Dockerfile.runner` (missing)
- `azure-container-apps-deploy.yml` → branch `azure-deploy` (missing)
- `validate-secrets.yml` → branch `hardening/all-phases` (missing)
- `bundle-size.yml` → `working-directory: base-branch` (missing dir)

## Permissions hardening

**Priority A — add explicit top-level `permissions: { contents: read }` to these 31** so they default to read-only instead of inheriting repo defaults:

```
accessibility, ai-model-pipeline, auto-fix, auto-merge, build, bundle-size,
ci-auth-debugger, codeql, commitlint, cost-optimization, coverage, deploy,
devonn-deploy, e2e, edge-functions-typecheck, ... (16 more — see full TSV)
```

**Priority B — review the 4 deploy-capable workflows for least-privilege:**
- `release.yml` — also pulls 3 secrets (GH_PAT, GITHUB_TOKEN, NPM_TOKEN). Verify GH_PAT is actually required.
- `platform-health-dashboard.yml`, `sbom-generation.yml`, `sbom.yml` — confirm `contents: write` is needed (probably only `contents: read` + `id-token: write` for OIDC).

**Priority C — secret-heavy review:**
- `terraform.yml` (14 secrets) and `validate-secrets.yml` (12) are expected.
- `deploy-and-publish.yml` (10) overlaps `deploy.yml` (7) and `oidc-deploy.yml` (8) — candidate for consolidation.

## Recommended PR sequence

1. **One PR**: strip `develop` from all 11 workflows (mechanical).
2. **One PR per dangling script/Dockerfile fix** (5 separate PRs for clean revert).
3. **One PR**: add `permissions: { contents: read }` defaults to the 31 workflows missing them.
4. **One PR**: scope-down `release.yml` and the 3 other deploy-capable workflows.
5. **Defer**: consolidating `deploy*.yml` triplet — needs Phase B design review first.

Full data: `reports/dead-paths.tsv`, `reports/permissions-audit.tsv`.

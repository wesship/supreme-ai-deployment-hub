# Hermes upstream staging

Use `update-hermes-upstream.sh` to install the pinned Nous Hermes Agent release into an isolated venv. Use `test-hermes-upstream.sh` for the version/config preflight.

Do not point the production `d3vonn-hermes` worker at this venv until the compatibility matrix passes.

apiVersion: v1
kind: Service
metadata:
  name: d3vonn-backend
  namespace: d3vonn
spec:
  type: ClusterIP
  selector:
    app: d3vonn-backend
  ports:
    - name: http
      port: 80
      targetPort: http

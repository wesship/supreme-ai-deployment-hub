apiVersion: v1
kind: ServiceAccount
metadata:
  name: d3vonn-backend
  namespace: d3vonn
  annotations:
    # IRSA — bind this ServiceAccount to an IAM role for scoped AWS access
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/d3vonn-backend-irsa

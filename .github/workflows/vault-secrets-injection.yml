name: Vault-Backed Secrets Injection (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  vault-status:
    name: Vault integration status
    runs-on: ubuntu-latest
    steps:
      - name: Explain manual-only mode
        run: |
          echo "External Vault OIDC injection is manual-only."
          echo "Production secrets remain managed by Railway, Vercel, GitHub Secrets, and the VPS environment."

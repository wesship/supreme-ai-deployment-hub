name: Deploy and Chrome Extension Publish (archived manual lane)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: Archived deployment lane
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical deployment lane
        run: |
          echo "This legacy deploy/publish workflow is intentionally manual-only."
          echo "Use .github/workflows/deploy.yml as the canonical deployment lane."
          echo "Do not add push triggers here."

name: Kubernetes Admission Enforcement (Kyverno)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kyverno-admission
  cancel-in-progress: true

jobs:
  control:
    name: Manual Kyverno Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          echo "Kyverno validation is manual-only because Kubernetes is not the active production platform."
          echo "Policy files remain preserved under policy/kyverno/."

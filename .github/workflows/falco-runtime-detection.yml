name: Falco Runtime Behavioral Detection

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: falco-runtime-detection
  cancel-in-progress: true

jobs:
  control:
    name: Manual Falco Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          echo "Falco validation is manual-only because Kubernetes runtime monitoring is not active in production."
          echo "Falco policies remain preserved under policy/falco/."

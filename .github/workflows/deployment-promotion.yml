name: Deployment Promotion Gates (archived manual lane)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: Archived promotion lane
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical deployment lane
        run: |
          echo "This legacy promotion workflow is intentionally manual-only."
          echo "Use .github/workflows/deploy.yml as the canonical deployment lane."
          echo "Do not add workflow_run or push triggers here."

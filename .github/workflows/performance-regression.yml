name: Performance Regression Detection (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  performance-status:
    name: Performance analysis status
    runs-on: ubuntu-latest
    steps:
      - name: Explain manual-only mode
        run: |
          echo "Performance regression analysis is manual-only."
          echo "Vercel production deployment checks remain active for normal pushes."

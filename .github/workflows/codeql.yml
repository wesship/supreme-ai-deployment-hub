name: CodeQL SAST

# Runs Static Application Security Testing on every push to main/develop
# and on all pull requests. Also runs on a weekly schedule to catch
# newly-discovered vulnerabilities in existing code.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Every Monday at 08:00 UTC
    - cron: '0 8 * * 1'

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  analyze:
    name: CodeQL Analysis (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language:
          - javascript-typescript   # covers all .ts, .tsx, .js files
          - python                  # covers backend/*.py and scripts/*.py

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # ── JavaScript/TypeScript setup ─────────────────────────────────────────
      - name: Setup Node.js (JS/TS analysis)
        if: matrix.language == 'javascript-typescript'
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: 22
          cache: npm

      - name: Install Node dependencies (JS/TS analysis)
        if: matrix.language == 'javascript-typescript'
        run: npm ci

      # ── Python setup ────────────────────────────────────────────────────────
      - name: Setup Python (Python analysis)
        if: matrix.language == 'python'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Python dependencies (Python analysis)
        if: matrix.language == 'python'
        run: |
          if [ -f backend/requirements.txt ]; then
            pip install -r backend/requirements.txt
          elif [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      # ── CodeQL Analysis ─────────────────────────────────────────────────────
      - name: Initialize CodeQL
        uses: github/codeql-action/init@ff0a06e83cb2de871e5a09832bc6a81e7276941f  # v3.28.18
        with:
          languages: ${{ matrix.language }}
          # Use the security-extended query suite for broader coverage
          queries: security-extended
          config: |
            paths-ignore:
              - node_modules
              - dist
              - build
              - .next
              - coverage
              - test-results
              - '**/*.test.ts'
              - '**/*.spec.ts'
              - '**/__tests__/**'

      - name: Autobuild
        uses: github/codeql-action/autobuild@ff0a06e83cb2de871e5a09832bc6a81e7276941f  # v3.28.18

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@ff0a06e83cb2de871e5a09832bc6a81e7276941f  # v3.28.18
        with:
          category: /language:${{ matrix.language }}
          upload: true
          output: sarif-results
          add-snippet: true

      - name: Upload SARIF results as artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        if: always()
        with:
          name: codeql-sarif-${{ matrix.language }}
          path: sarif-results/
          retention-days: 14

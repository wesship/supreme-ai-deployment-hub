name: Predictive Failure Detection (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  predictive-status:
    name: Predictive failure status
    runs-on: ubuntu-latest
    steps:
      - name: Explain manual-only mode
        run: |
          echo "Predictive failure analysis is manual-only."
          echo "Core production validation remains handled by Railway, Vercel, testing, and smoke checks."

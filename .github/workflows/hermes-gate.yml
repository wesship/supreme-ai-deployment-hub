name: Governance Review

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Manual review placeholder
        run: echo "Manual governance review placeholder for stabilization."

name: AI Safety Guardrails (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ai-safety-status:
    name: AI safety audit status
    runs-on: ubuntu-latest
    steps:
      - name: Explain manual-only mode
        run: |
          echo "Extended AI safety guardrail audits are manual-only."
          echo "Core application tests and production deployment checks remain active."

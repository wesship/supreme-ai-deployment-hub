apiVersion: v1
kind: ServiceAccount
metadata:
  name: devonn-backend
  namespace: devonn
  annotations:
    # IRSA — bind this ServiceAccount to an IAM role for scoped AWS access
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/devonn-backend-irsa
